#include <stdio.h> // include the standard i/o library for printing to console

/*
 * all programs start at main
 * there can only be one main
 */

int main()
{
    // a string literal is anything enclosed in ""
    printf("hello, world!\n"); // printf is used to print to the console

    return /* a comment in the middle of a line */ 0; // 0 means success when returned from main (successful execution of code)
}